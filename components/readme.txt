Quick Rundown on components files:
this is where you'll put reusable bits of code
you can then export it in index.js and call
them using import in other files
for example:
import { CustomButton, Loader } from "@/components"; in index.jsx
-Sophia