//this is where the files are exported out to use throughout the rest of the app
//if you create another component file be sure to export it here!
import Loader from "./Loader";
import CustomButton from "./CustomButton"
export {
    Loader,
    CustomButton
}